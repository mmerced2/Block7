//Creatign string for user regarding secret combination
console.log("You have received this message because you have been chosen to open an important vault. Here is the secret combination:")

//creating the variables that make up my combination resulting from my own caculations
const variable1 = 5+5;
const variable2 = 4*10;
const variable3 = 40-1;
const combination = `${variable1} - ${variable2} - ${variable3}`;

//creatign the alert for the user with secret combination
alert(`The combination of the vault is: ${combination}`);

//displaying combination
console.log(`The combination of the vault is:" ${combination}`);

